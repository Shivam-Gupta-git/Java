//What is a Variable.........
// -> Variable = used to store data and value stored in the computer’s memory
/* 1. Local Variables = Declared inside methods, constructors or blocks. Only accessible within that method/block.
    public void display() {
    int number = 10;  // local variable
    System.out.println(number);
 } */

/* 2. Instance Variables = Declared inside a class but outside any method.
    class Student {
    String name;   // instance variable
    int age;       // instance variable
} */

/* 3. Static Variables (Class Variables) = Declared with the static keyword
    class Student {
    static String college = "ABC University";  // static variable
} */

// Data types...................
// -> data type =  a data type defines the kind of data a variable can hold and how much memory it uses.
// -> Types of Variables in Java.

// data types are mainly divided into two categories
/* 1. Primitive Data Types => byte(1 byte), short(2 bytes), int(4 bytes), long(8 bytes), float(4 bytes),
double(8 bytes), char(2 bytes), boolean(1 bit) */

/* 2. Non-Primitive Data Types => Objects like String, Arrays, Classes, Interfaces
      note -> 1 byte = 8 bits */
