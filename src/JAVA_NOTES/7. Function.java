//Function.................
/* -> Function = Java is a block of code inside a class that performs a specific task,
      can take input (parameters), and may return a value
*/
/*
	•	modifier → access level (public, private, protected, default)
	•	returnType → type of value returned (int, String, void, etc.)
	•	methodName → name of the function
	•	parameters → inputs (optional)
	•	return → gives back a value (if not void)
*/

// -> There are two types of parameters in our function
// 1. Parameters (formal parameters)
// 2. actual Parameters (arguments)

// -> Call by value........................
/*    all method arguments are passed by value, which means a copy of the variable is passed to the function (method).
      So, any changes made inside the method do not affect the original variable outside the method.
*/

// -> There are two types of method.
// 1. user define method = that type of method. which can defined by user.
// 2. inbuilt method = that type of method. which can all ready defined.

// -> Function Overloading = multiple functions with the same name but different parameters.